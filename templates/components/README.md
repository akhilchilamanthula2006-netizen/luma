# Luma Reusable Components Directory
Contains template pieces or macros (like custom forms, alert messages, etc.) that can be imported or included in pages.
Currently, the shared layout wrapper resides in `templates/base.html`.
